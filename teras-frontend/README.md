# TERAS Frontend

## Démarrage
1. `npm i`
2. copier `.env.example` → `.env.local` et ajuster `VITE_API_BASE`
3. `npm run dev`

## Pages clés
- Dashboard, Login (déjà présents)
- ConfigManager, ProfileSwitcher, ComputeScore, RegionsCountries (consomment l'API TERAS)
